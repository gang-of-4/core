/** @type {import('next').NextConfig} */
const nextConfig = {
    transpilePackages: ['ui'],
    basePath: '',
    images: {
        domains: ['via.placeholder.com'],
    },
}

module.exports = nextConfig
