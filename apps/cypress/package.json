{
  "name": "cypress",
  "version": "0.1.0",
  "main": "index.ts",
  "scripts": {
    "cy:run": "cypress run",
    "cy:e2e": "cypress open",
    "cy:run-unit": "cypress run --spec cypress/e2e/unit",
    "cy:run-integration": "cypress run --spec cypress/e2e/integration"
  },
  "devDependencies": {
    "cypress": "^13.4.0"
  }
}
