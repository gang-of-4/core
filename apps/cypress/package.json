{
  "name": "cypress",
  "version": "0.1.0",
  "main": "index.ts",
  "scripts": {
    "dev": "node index.ts",
    "cypress-open": "cypress open",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "cypress": "^13.3.0"
  }
}
